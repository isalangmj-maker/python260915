# 고객정보 관리 프로그램 PRD

## 1. 문서 개요

### 1.1 목적

Python의 SQLite3와 PyQt6를 사용해 고객정보를 입력, 조회, 수정, 삭제할 수 있는 데스크톱 프로그램을 개발한다.

### 1.2 산출물

| 산출물 | 설명 |
| --- | --- |
| `MyCust.py` | 고객정보 관리 프로그램의 실행 파일 |
| `MyCust.db` | 고객정보를 저장하는 SQLite 데이터베이스 파일 |
| `Customers` | 고객정보를 저장하는 SQLite 테이블 |

> 현재 단계에서는 설계와 요구사항만 정리한다. `MyCust.py`와 `MyCust.db`는 기능 구현 단계에서 생성한다.

## 2. 기술 요구사항

- Python 3
- SQLite3 표준 라이브러리
- PyQt6 GUI 프레임워크
- 데이터베이스 파일명: `MyCust.db`
- 프로그램 파일명: `MyCust.py`
- SQL 실행 시 값은 문자열 조합이 아니라 매개변수 바인딩을 사용한다.
- 프로그램 실행 시 데이터베이스와 테이블이 없으면 자동으로 생성한다.

## 3. 데이터베이스 설계

### 3.1 테이블 정의

테이블명은 `Customers`로 한다.

| 컬럼명 | SQLite 타입 | 제약조건 | 설명 |
| --- | --- | --- | --- |
| `custID` | `INTEGER` | `PRIMARY KEY AUTOINCREMENT` | 고객 식별자. 입력 시 자동 증가 |
| `custName` | `TEXT` | `NOT NULL` | 고객 이름 |
| `custTitle` | `TEXT` | `NOT NULL` | 고객 직함 |

### 3.2 논리적 생성 기준

```sql
CREATE TABLE IF NOT EXISTS Customers (
    custID INTEGER PRIMARY KEY AUTOINCREMENT,
    custName TEXT NOT NULL,
    custTitle TEXT NOT NULL
);
```

### 3.3 데이터 처리 원칙

- `custID`는 사용자가 직접 입력하지 않고 데이터베이스가 생성한다.
- 신규 고객 등록 후 생성된 `custID`를 화면에 반영한다.
- 수정과 삭제는 선택된 `custID`를 기준으로 수행한다.
- 목록은 `custID` 오름차순으로 표시한다.
- 데이터베이스 연결은 프로그램 종료 시 정상적으로 닫는다.

## 4. 클래스 설계

### 4.1 `CustomManger` 클래스

고객 데이터와 SQLite 연결을 담당하는 데이터 접근 클래스이다. 클래스명은 요청사항의 표기를 그대로 사용한다.

#### 책임

- `MyCust.db` 연결 생성 및 유지
- `Customers` 테이블 초기화
- 고객 데이터 CRUD 처리
- SQL 예외를 호출 화면에서 처리할 수 있는 형태로 전달
- 데이터베이스 연결 종료

#### 주요 메서드

| 메서드 | 역할 | 반환 기준 |
| --- | --- | --- |
| `__init__(database_path="MyCust.db")` | 데이터베이스 연결 및 테이블 준비 | 없음 |
| `create_customer(cust_name, cust_title)` | 고객 신규 입력 | 생성된 `custID` |
| `update_customer(cust_id, cust_name, cust_title)` | 기존 고객 수정 | 수정 성공 여부 |
| `delete_customer(cust_id)` | 기존 고객 삭제 | 삭제 성공 여부 |
| `get_customers(keyword="")` | 고객 목록 조회 및 검색 | 고객 행 목록 |
| `close()` | 데이터베이스 연결 종료 | 없음 |

#### 검색 기준

- 검색어가 비어 있으면 전체 고객을 조회한다.
- 검색어가 숫자이면 `custID` 일치 또는 `custName`/`custTitle` 부분 일치 결과를 조회한다.
- 검색어가 숫자가 아니면 `custName` 또는 `custTitle`의 부분 일치 결과를 조회한다.
- 검색 결과가 없으면 빈 목록을 반환한다.

### 4.2 `CustomerView` 클래스

PyQt6 화면 구성과 사용자 입력, 버튼 이벤트, 목록 표시를 담당하는 화면 클래스이다.

#### 책임

- 입력 컨트롤과 버튼 배치
- 사용자 입력값 검증
- `CustomManger` 호출
- CRUD 처리 결과와 오류 메시지 표시
- `QTableWidget`에 고객 목록 표시
- 테이블 행 선택 시 선택한 고객정보를 입력 컨트롤에 반영
- 검색 결과 갱신 및 입력 컨트롤 초기화

#### 화면 상태

- 신규 입력 상태: `custID`는 비어 있거나 읽기 전용으로 표시한다.
- 선택 상태: 테이블에서 행을 선택하면 `custID`, 고객 이름, 직함을 입력 영역에 표시한다.
- 수정/삭제 상태: 선택된 고객의 `custID`를 기준으로 처리한다.
- 처리 완료 후 목록을 새로 조회하고 필요하면 입력 영역을 초기화한다.

## 5. 화면 설계

### 5.1 전체 배치

PyQt6 기반의 메인 윈도우를 사용하며 화면을 다음 영역으로 나눈다.

```text
+----------------------+----------------------------------+
| 작업 버튼 영역        | 고객 입력 영역                    |
|                      |                                  |
| [입력]               | 고객 ID: [읽기 전용 입력]         |
| [수정]               | 고객 이름: [입력 컨트롤]          |
| [삭제]               | 직함: [입력 컨트롤]               |
| [검색]               | 검색어: [입력 컨트롤]             |
+----------------------+----------------------------------+
| 고객 목록(QTableWidget)                              |
| 고객 ID | 고객 이름 | 직함                            |
+------------------------------------------------------+
```

### 5.2 왼쪽 작업 영역

왼쪽에는 다음 버튼을 세로로 배치한다.

| 버튼 | 동작 |
| --- | --- |
| 입력 | 고객 이름과 직함을 검증한 뒤 신규 고객을 저장 |
| 수정 | 선택한 `custID`의 고객 이름과 직함을 수정 |
| 삭제 | 선택한 고객을 확인 후 삭제 |
| 검색 | 검색어를 기준으로 고객 목록을 조회 |

### 5.3 오른쪽 입력 영역

- 고객 ID 입력 컨트롤: 읽기 전용. 데이터베이스에서 생성된 값을 표시한다.
- 고객 이름 입력 컨트롤: 필수 입력.
- 고객 직함 입력 컨트롤: 필수 입력.
- 검색어 입력 컨트롤: 고객 ID, 이름, 직함 검색에 사용한다.
- 검색어 입력 후 `검색` 버튼을 클릭하거나 Enter 키를 입력하면 검색한다.

### 5.4 하단 목록 영역

하단에는 `QTableWidget`을 배치한다.

- 열 수: 3개
- 열 제목: `고객 ID`, `고객 이름`, `직함`
- 데이터베이스의 `custID`, `custName`, `custTitle`을 표시한다.
- 셀 직접 편집은 허용하지 않는다.
- 한 번에 한 행을 선택한다.
- 행을 클릭하면 오른쪽 입력 컨트롤에 해당 행의 데이터가 표시된다.
- 조회 결과가 없으면 빈 목록을 표시하고 안내 메시지를 보여준다.

## 6. 기능 요구사항

### FR-01. 프로그램 시작

1. 프로그램은 `MyCust.db`에 연결한다.
2. `Customers` 테이블이 없으면 정의에 맞게 생성한다.
3. 전체 고객 목록을 `QTableWidget`에 표시한다.

### FR-02. 고객 입력

1. 고객 이름과 직함이 모두 입력되었는지 확인한다.
2. 앞뒤 공백을 제거한 값을 저장한다.
3. 저장 성공 후 목록을 갱신한다.
4. 성공 여부를 사용자에게 알리고 다음 입력을 위해 입력 영역을 정리한다.

### FR-03. 고객 수정

1. 테이블 행 또는 고객 ID가 선택되어 있는지 확인한다.
2. 고객 이름과 직함을 검증한다.
3. 해당 `custID`의 레코드를 수정한다.
4. 수정 성공 후 목록을 갱신하고 결과를 알린다.

### FR-04. 고객 삭제

1. 삭제할 고객이 선택되어 있는지 확인한다.
2. 삭제 전 확인 대화상자를 표시한다.
3. 사용자가 확인한 경우 해당 `custID`를 삭제한다.
4. 삭제 후 목록을 갱신하고 입력 영역을 초기화한다.

### FR-05. 고객 검색

1. 검색어를 기준으로 고객 ID, 이름, 직함을 검색한다.
2. 검색 결과를 `QTableWidget`에 다시 표시한다.
3. 검색어가 비어 있으면 전체 목록을 표시한다.

### FR-06. 입력 검증 및 오류 처리

- 고객 이름이 비어 있으면 저장 또는 수정하지 않고 안내한다.
- 고객 직함이 비어 있으면 저장 또는 수정하지 않고 안내한다.
- 수정 또는 삭제할 고객이 선택되지 않았으면 작업을 수행하지 않는다.
- 데이터베이스 오류가 발생하면 사용자에게 오류 내용을 알리고 프로그램이 중단되지 않도록 한다.

## 7. 비기능 요구사항

- 데이터는 프로그램을 종료한 뒤에도 `MyCust.db`에 보존되어야 한다.
- 모든 SQL은 매개변수 바인딩을 사용해 안전하게 실행한다.
- 화면 클래스는 SQL문을 직접 실행하지 않으며, 데이터베이스 작업은 `CustomManger`에 위임한다.
- `CustomManger`는 PyQt6 위젯을 직접 조작하지 않는다.
- 기존 프로젝트의 Python 및 PyQt6 사용 방식과 일관성을 유지한다.
- 프로그램은 Windows 환경에서 실행할 수 있어야 한다.

## 8. 완료 기준

- [ ] `MyCust.py`가 실행된다.
- [ ] 실행 시 `MyCust.db`와 `Customers` 테이블이 자동으로 준비된다.
- [ ] `custID`가 자동 증가 기본키로 동작한다.
- [ ] 고객 입력, 수정, 삭제, 검색이 동작한다.
- [ ] 왼쪽에 입력/수정/삭제/검색 버튼이 배치된다.
- [ ] 오른쪽에 고객 ID, 고객 이름, 직함, 검색어 입력 컨트롤이 배치된다.
- [ ] 하단 `QTableWidget`에 고객 목록이 표시된다.
- [ ] 테이블 행 선택 정보가 입력 영역에 반영된다.
- [ ] `CustomManger`와 `CustomerView`의 책임이 분리되어 있다.
- [ ] 프로그램 종료 시 데이터베이스 연결이 닫힌다.

## 9. 구현 범위 제외

- 회원 인증 및 권한 관리
- 고객별 주소, 전화번호, 이메일 등 추가 정보
- 네트워크 데이터베이스 또는 다중 사용자 동시 편집
- 엑셀 가져오기 및 내보내기
- 고객정보 암호화
- 현재 단계에서의 실제 Python 코드 및 데이터베이스 파일 생성